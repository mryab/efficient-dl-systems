FROM python:3.13-slim

WORKDIR /srv

# Cache-hostile: copy entire context before dependency install.
COPY . /srv

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8080

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8080"]
